import 'package:flutter/material.dart';

class Categories_screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Categories_screen.",style: Theme.of(context).textTheme.bodyText1,),

    );
  }
}
