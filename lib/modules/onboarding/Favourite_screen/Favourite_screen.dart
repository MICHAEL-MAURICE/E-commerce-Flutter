import 'package:flutter/material.dart';

class Favourite_screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Favourite_screen.",style: Theme.of(context).textTheme.bodyText1,),

    );
  }
}
