import 'package:flutter/material.dart';

class Search_screen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Search_screen",style: Theme.of(context).textTheme.bodyText1,),

    );
  }
}
